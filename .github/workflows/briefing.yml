name: Daily Briefing

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  briefing:
    runs-on: ubuntu-latest

    steps:
      - name: Schedule test
        run: |
          echo "Scheduled workflow is running"
